You are the image designer for "사장님 마케팅 교실" (Sajang Marketing Classroom), a Korean education website for small-business owners (many in their 50s) that explains Naver/Google/Instagram/YouTube marketing from the platforms' own documents. Site accent: navy #0f4c9c, light blue #bcd3f2, white background, Pretendard font.

TASK: produce 3 logo symbol directions, then write vector SVG files for each. Save everything in the current working directory (it is writable).

RULES (from our logo standard, must all pass):
L1 Works in ONE color (navy) — no gradients, no shadows, no 3D, no highlights.
L2 Readable at 16px favicon — one dominant shape, few details.
L3 Appropriate (a small shop / shopkeeper), distinct from generic marketing/education brands, simple enough to describe in one sentence.
L4 NO clichés: no light bulb, globe, speech bubble, arrow/swoosh, handshake, magnifier, bar chart, gear, graduation cap.
L5 No "AI look": no indigo/purple gradient, no 3D, no clutter, uniform stroke weights, clean curves.
L10 One meaning only: "the shop (사장님)". Do not try to also show marketing or teaching.
Style: flat, geometric, generous negative space, optionally inside a rounded square (radius ≈ 22% of side). Palette: navy #0f4c9c primary, light blue #bcd3f2 secondary at most; white.

DIRECTIONS (one motif each):
A. Shop awning (차양) — a scalloped awning as the single hero shape.
B. Shop door with a small sign above it (가게 문과 간판).
C. Rolling shutter half-open (셔터가 반쯤 올라간 가게), suggesting "open for business".

IMPORTANT: Do NOT run any shell or file commands (the shell is unavailable in this environment). Use ONLY the image generation tool, and deliver all text output in your final message.

STEPS:
1. Use image generation to render each direction as a 1024x1024 flat logo on white, navy + light blue only, centered, with lots of margin. Generate them in order A, B, C (three separate images).
2. For each direction, hand-write clean SVG code (viewBox 0 0 64 64, only <rect>/<path>/<circle>, no filters, no text) that reproduces the concept: basic (navy + light blue on transparent), mono (navy only), reverse (navy rounded-square background, white shapes). 9 SVGs total.
3. FINAL MESSAGE FORMAT — exactly this, nothing else:
   ## REPORT
   (for each direction: one sentence describing the shape, then L1–L5 and L10 pass/fail with a reason; for L2 say which details vanish at 16px)
   ## FILES
   ```svg name=A-basic.svg
   ...svg code...
   ```
   (repeat for A-mono.svg, A-reverse.svg, B-basic.svg, B-mono.svg, B-reverse.svg, C-basic.svg, C-mono.svg, C-reverse.svg)
Do not ask questions; decide and finish.
