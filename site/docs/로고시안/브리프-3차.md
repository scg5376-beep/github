Round 3, final vector for the "사장님 마케팅 교실" logo. The attached image is the chosen concept A2 (four-scallop awning, flared/trapezoid silhouette: narrower at the top, wider at the bottom, stripes navy #0f4c9c / light blue #bcd3f2 alternating, no top bar).

Your round-2 SVG did not match it: the stripes were straight-sided (rectangular), not flared, and the awning was too squat. Fix that. Do NOT run shell/file commands; put SVG code in the final message only. No new raster needed.

Write, with viewBox 0 0 64 64 and only <path>/<rect>:
1. awning-basic.svg — the flared awning exactly like the attachment: top edge ≈ x 14..50 at y 18, bottom scallop tips reaching ≈ x 8..56 at y 46; four stripes (navy, light, navy, light) each a flared quadrilateral ending in a semicircular scallop; stripes share edges with no gaps; uniform geometry. Transparent background.
2. awning-mono.svg — same silhouette, navy only (stripes merged into one shape).
3. awning-reverse.svg — <rect width="64" height="64" rx="14" fill="#0f4c9c"/> plus the same awning scaled to ~72% and centered, white stripes alternating with #bcd3f2.
4. awning-16.svg — favicon version: same flared silhouette with 3 scallops, single navy color, no stripes, filling ~85% of the box.
Final message format, exactly:
## FILES
```svg name=awning-basic.svg
...
```
(then awning-mono.svg, awning-reverse.svg, awning-16.svg)
Decide and finish; no questions.
