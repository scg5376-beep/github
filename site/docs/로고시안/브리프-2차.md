Round 2 for the "사장님 마케팅 교실" logo. The attached image is direction A (scalloped shop awning) from round 1 — it was selected. Your round-1 SVG for A was much weaker than the raster (a thin strip). Fix that.

Do NOT run shell/file commands. Use only the image generation tool, and put all SVG code in the final message.

TASKS:
1. Generate 2 refined raster variants of the awning, 1024x1024, flat, navy #0f4c9c + light blue #bcd3f2 on white, centered, generous margin, no gradient/shadow/3D:
   A1 — awning with a straight top valance bar and 5 scallops, alternating navy / light blue stripes, proportions like the attached image but slightly bolder and more geometric.
   A2 — same awning but only 4 scallops and NO top bar, stripes navy/light blue, drawn so it still reads at 16px (thicker scallops).
2. Write faithful SVG code (viewBox 0 0 64 64; only <rect>/<path>; uniform, clean curves; the awning should fill about 70% of the width and sit vertically centered) for the better of A1/A2, in three versions:
   awning-basic.svg (navy + light blue on transparent), awning-mono.svg (navy only), awning-reverse.svg (navy rounded square rx=14 filling 0..64, white awning with light-blue stripes).
   Also awning-16.svg: a simplified 16px version (3 scallops, no stripes) for the favicon.
3. Final message format, exactly:
   ## CHOICE
   (A1 or A2 and one sentence why; which details vanish at 16px)
   ## FILES
   ```svg name=awning-basic.svg
   ...
   ```
   (then awning-mono.svg, awning-reverse.svg, awning-16.svg)
Decide and finish; no questions.
