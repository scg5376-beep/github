# 미정 폴더 (마스터피스)

유형(캐릭터/룩북/배경/카메라구도/원근설정)을 정하지 못한 카드가 여기에 저장됩니다.
사용자가 유형을 알려주지 않았을 때만 사용하며, 나중에 반드시 제자리로 옮겨야 합니다.

## 옮기는 방법
1. 카드의 `type` 을 올바른 값으로 수정한다.
2. `id` 를 해당 유형의 다음 번호로 바꾼다 (예: `UN-아리` → `CH-002`).
3. 파일을 `masterpieces/<유형폴더>/` 로 옮긴다.
4. `./mp index` 로 인덱스를 갱신한다.
