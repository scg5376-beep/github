# 이 저장소의 규칙 — 먼저 `docs/길라잡이.md` 를 연다

- 어떤 일을 할 때 어떤 규칙을 읽는지는 **`docs/길라잡이.md`** 한 장에 표로 있다. 규칙 본문은 `docs/규칙/` 에 일 하나당 파일 하나.
- 시작은 `docs/규칙/공통_시작.md`, 끝은 `docs/규칙/공통_마무리.md`(최종 검수·기록·좀비 프로세스 정리·보고). 예외 없다.
- 규칙을 여기에 적지 않는다. 여기는 길만 가리킨다. (운영자 2026-09-16)
