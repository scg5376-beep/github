# 버그리포트 2026-09-19 (162~185바퀴)

> 운영자: "나 이제 자러갈테니까 작업 끝나거나 중단되면 기록하고 버그리포트 작성하고 완성도체크해서 필요하면 커밋해도좋아"
> 이틀(09-18·09-19) 동안 만난 오류·결함과 처리. 열린 것은 맨 아래.

## 고친 것

| 번호 | 증상 | 원인 | 처리 | 재발 방지 |
|---|---|---|---|---|
| B1 | 표 격자·「한 줄 답」 테두리를 CSS 에 넣었는데 캡처에 안 보임 | `style.css` 링크에 버전이 없어 헤들리스 Edge 프로필(과 방문자 브라우저)이 옛 CSS 를 씀 | `build.py` `css_ver()` — `style.css?v=<sha8>` (D73) | 캡처 눈검사 전에 `?v=` 값이 바뀌었는지 본다 |
| B2 | 리모컨(우측 기둥) 「다른 곳」 칩이 2열로 안 놓임 | `aside.rail ul.cats li` 규칙이 새 grid 규칙보다 우선순위가 높았음 | 선택자를 `aside.rail ul.cats li.others` 로 올림 | 기존 규칙 우선순위를 먼저 본다 |
| B3 | 인용 줄(`p.cite`) 이 검사기 S2-16·T3·T4·문단 규칙에 걸림 | 검사기가 새 클래스를 본문으로 셈 | `check.py` 4곳에 `cite` 제외 | 새 클래스는 검사기 제외 목록도 같이 |
| B4 | `ht_daangn.py` 문자열 안에 실제 줄바꿈이 들어가 빌드 깨짐 | Bash heredoc 이 `\n` 을 실제 줄바꿈으로 바꿈. Bash-python 치환은 저장 안 됨 | Edit 도구로 고침 | 문자열 안 줄바꿈 오류는 Edit 로 |
| B5 | 138바퀴 커밋이 check 오류 1(S2-17) 인 채 push → CI 실패 | 체인 명령에서 check 결과를 보지 않고 커밋 | 다음 커밋으로 정정 | 「오류 0」을 눈으로 본 뒤 커밋. 체인 금지 |
| B6 | Q1 인용 대조 실패 | 원문이 여러 줄인 것을 한 덩어리로 인용 | 한 줄만 인용 | 인용은 원문 한 줄 단위 |
| B7 | GA4 켜기 실험 뒤 임시 ID 가 남을 뻔함 | 더미 ID 로 켜서 검사 | 되돌린 뒤 실제 ID 입력, `ga_probe.mjs` 로 요청 2건·오류 0 확인 | 실험 값은 같은 회차에 되돌린다 |
| B8 | 「한 줄 답」이 목차 아래로 밀림(400폭) | lead 블록 밖에 넣음 | lead 블록 안으로(D70) | 400폭 캡처 눈검사 |
| B9 | `mkurls.py` 인자 없이 실행하면 실패, `audit.mjs` 는 8765 서버 없으면 0쪽 | 사용법 기억 오류 | site/ 에서 `mkurls.py <out>` → 서버 확인 → audit | 공통_마무리 표에 적힘 |
| B11 | D73 뒤 첫 전수 검사에서 8쪽 가로 넘침(sheet 표 5·start·agency-call·en) | `table.sheet` 의 620px `display:table` 되돌림(D60) + 격자 규칙 `overflow:hidden`·`nowrap` 이 좁은 화면 규칙을 덮음. 09-18 「전수 0」은 옛 CSS 캐시를 본 것 | D74: 820px 이하 모든 표 옆으로 밀기, 줄 머리글만 nowrap | 캐시 버전 뒤 수치만 믿는다 |
| B12 | 커밋 8a2eb1d CI 실패 「빌드 결과가 커밋과 같은지」 — `style.css?v=` 값이 로컬 9a6b3528 / CI e7bbaf38 | Python `write_text` 가 윈도우에서 CSS 를 CRLF 로 저장 → 로컬 해시는 CRLF 기준, git 이 LF 로 정규화해 CI 해시가 다름 | `css_ver()` 가 CRLF 를 LF 로 맞춘 뒤 해시 | 파일 쓸 때 `newline="
"`. 같은 자리에서 heredoc `
` 이 또 실제 줄바꿈이 됨(B4 재발) → Edit 로 고침 |
| B13 | 커밋 e1336ea CI 실패(빌드 결과 불일치: guide/threads.html·sitemap.xml). 다음 커밋 fd61713 부터는 성공 | 확인: 그 커밋에서 바뀐 threads.html 의 수정일을 로컬 빌드는 「마지막 커밋 날짜(이전 커밋)」로, CI 는 「새 커밋 날짜」로 계산 | build.py git_dates: 커밋 안 한 수정본은 오늘 날짜 | 커밋 직후 CI 결과를 본다 |
| B10 | 알약이 「Generic.JS.Obfs」 탐지(운영자 캡처) | 저장소 스크립트엔 난독화 없음. 헤들리스 Edge 임시 프로필 캐시로 추정, 경로 미확인 | 운영자가 치료 | 캡처 뒤 `cleanup.py` 로 프로필 정리 |

## 못 읽은 원문 (건너뜀)

| 대상 | 상태 |
|---|---|
| 네이버 도움말 20417(필터 기능) | 본문 추출 실패 |
| 네이버 도움말 20437(맛집 정답형) | 404 |

## 열린 것 (운영자 몫 → `docs/발전/운영자할일.md`)

- 네이버 색인 1쪽뿐, 목표 검색어 0/30 노출. 서치어드바이저 수집요청은 로그인이 필요해 내가 못 한다.
- GA4 수치는 내가 읽을 권한이 없다. 캡처로 받는다.

## 완성도 점검 (185바퀴 마감)

- `check.py`: 109쪽 오류 0 · 경고 429
- 레이아웃 전수(120 URL × 320/400/768/1366): 아래 검수기록 참조
- tone.json 에 im-not-ai 대조 규칙 4개(S1-15) 추가 — 본문 걸림 0, 구글 인용문 안 2건은 정상 제외

## 2차 실행(186~223바퀴, 운영자 「작업해」 뒤) 에서 만난 것

| 번호 | 증상 | 원인 | 처리 | 재발 방지 |
|---|---|---|---|---|
| B14 | 문단 나누기 첫 판이 「등록은 무료니까 / 다른 무엇보다」처럼 문장 중간을 잘랐다 | 검사기와 같은 `[.다요까!?]\s` 경계를 썼는데 「~니까 」 어미가 문장 끝으로 잡힘 | `paras.py` 는 마침표·물음표·느낌표 뒤 공백만 자르고 마침표는 앞 문단에 남김 | 자동 편집은 미리보기(--dry)로 3쪽 이상 눈으로 본 뒤 적용 |
| B15 | 네이버 도움말이 5초 대기로는 메뉴만 오고 본문이 안 옴(앞서 20417 「추출 실패」의 원인) | 본문이 스크립트로 늦게 열림 | `?osType=COMMONOS` + 15초 대기 | README 에 적음 |
| B16 | 검사기 P1 「대전」이 「휴대전화」에 걸림 | 지역명 패턴에 앞 글자 조건이 없음 | `(?<!휴)대전` | 지역명 패턴은 낱말 경계를 같이 본다 |
| B17 | 색인 글 목차가 「1. 업체 등록, 2. 앱·알림…, 3. 정보 수정」 순으로 어긋남 | 절 제목 앞 숫자를 문자열로 정렬(「10.」이 「1.」 뒤) | 절 제목에서 숫자를 빼고 순서를 고정 | 목차 번호는 빌드가 붙이니 제목에 숫자를 넣지 않는다 |
| B18 | 첫 화면 색인 카드에 「25분 · 돈 듦」 | 카드 꼬리표가 글의 분류(네이버/광고)로 정해짐 | 색인 글(url 에 help-index·faq-)은 「참고」 | 새 글 종류가 생기면 첫 화면 꼬리표를 눈으로 본다 |
| B19 | 예약주문 사업자 고객센터(11712) 도움말 번호를 못 찾음(20690~20760 빈 번호) | 항목이 스크립트로 열리고 번호 대역이 다름 | 큐 20·운영자할일(주소 4개 복사 요청) | |
| B20 | 새 글의 외부 링크를 curl 로 검사하니 help.naver.com·korea.kr·heraldcorp 가 전부 000 | 이 환경에서 curl 이 그 호스트에 못 붙음(WebFetch 와 같은 차단) | 헤들리스로 본문을 받은 기록으로 갈음 | 링크 검사는 헤들리스 제목 확인으로 |
| B21 | `ss_faq.py` 첫 판이 본문을 못 뽑음(10자) | 답 본문 `div.cus_answer` 가 `</li>` 로 닫히지 않음 | `cus_answer` 부터 `reply_check_box` 앞까지로 | 새 사이트 파서는 한 건 결과를 눈으로 본 뒤 돌린다 |
| B22 | 빌드 파이썬을 heredoc 로 고칠 때 ``·`
` 이 실제 문자로 들어감(B4 계열, 이번엔 `<h2` 가 백스페이스) | Bash heredoc 이 역슬래시 시퀀스를 해석 | 정규식이 든 코드는 Edit 도구로 | 규칙: 코드 파일의 정규식·이스케이프는 heredoc 금지 |
| B23 | 커밋 e1336ea CI 실패(threads.html 수정일 불일치, B13) | git_dates 가 커밋 안 한 파일의 날짜를 이전 커밋 날짜로 봄 | 커밋 안 한 수정본은 오늘 날짜 | 커밋 직후 CI 결과를 본다 |
| B24 | 전수 검사 URL 목록에 /guide/smartplace-help-, /guide/kakao-guide- 같은 잘린 주소 | `mkurls.py` 가 경로의 「index.html」을 전부 빈 문자열로 치환(파일명 속 index.html 도) | 끝의 /index.html 만 떼도록 | 새 파일명에 index 가 들어가면 URL 목록을 눈으로 본다 |
| B25 | 전수 검사가 「unsettled top-level await」로 죽고(exit 13), 다른 실행은 ECONNREFUSED 9334 | 4초 고정 대기 안에 Edge 디버그 포트가 안 열림(다른 Edge 프로세스 20개 이상) · 죽은 탭의 응답을 시한 없이 기다림 | 포트 대기 30초 재시도, 호출당 25초 시한, URL 별 오류 기록. 138 URL 은 4묶음으로 나눠 돌림 | 긴 전수 검사는 묶음으로 |
| B26 | heredoc 안 파이썬에서 역슬래시가 또 사라짐(B22 와 같음, `r""` → 제어문자) | Bash 도구 heredoc 이 역슬래시를 해석 | `chr(92)` 로 우회하거나 Edit 도구 | 코드 안 역슬래시는 heredoc 금지(규칙으로 올림) |


## B27 regen.py 가 생성기 하나의 SyntaxError 를 삼킴 (2026-09-20, 233바퀴)
- 문자열 index 로 sources 끝을 잡다가 다른 page() 의 desc 안에 끼워 넣어 `ht_daangn.py` 가 SyntaxError. `regen.py | tail -1` 만 봐서 못 봤고 build·check 는 옛 `_src` 로 오류 0 이 나옴. 커밋까지 됐음(bec7492).
- 고침: 다음 커밋에서 바로잡음. 규칙: regen 결과는 `grep -v " ok$"` 로 실패 줄을 본다. 파이썬 생성기 편집은 앵커 문자열 전체(닫는 괄호 포함)로 잡는다.

## B28 cat 이 TAXO 에 없는 설명 글 9쪽이 /guide/ 전체 목록에서 빠져 있었음 (2026-09-20, 246바퀴)
- 「네이버/예약」「네이버/광고」「네이버/스토어」「당근」「구글」처럼 TAXO 에 없는 cat 을 쓴 글(academy·hair-salon·reservation-settlement·agency-contract·naver-ads-help-index·quick-settlement·daangn-reviews·google-suspended·reservation-help-index)은 빌드는 되지만 `/guide/` 와 플랫폼 페이지 목록에 안 실림. 첫 화면 카드로만 닿았음. 최소 09-19 부터.
- 고침: TAXO 네이버에 「예약」 채널 추가, 나머지는 있는 채널로 바꿈(파워링크·판매/스마트스토어·당근/비즈프로필·구글/검색). `read_pages()` 에서 cat 이 TAXO 에 없으면 sys.exit 로 빌드를 멈추게 함. 결과 /guide/ 목록 50→59쪽.

## B29 수집요청 1순위 목록에 새 글 13개가 빠져 있었음 (2026-09-20, 마무리)
- 매 회차 `s.replace("<직전 마지막 주소>\n```", …)` 로 붙였는데 첫 회차 뒤로는 앵커가 안 맞아 조용히 실패(assert 없음). 헤더 수만 올라가고 주소는 47+1 그대로.
- 고침: 마무리 때 1순위 블록을 다시 세어 13개를 추가, 61개로 맞춤. 교훈: 목록에 붙일 때는 앵커 대신 블록 끝을 찾아 붙이고 붙인 뒤 수를 센다.

## B30 check 오류 1인 채로 커밋됨 (2026-09-20, 286바퀴)
- `check.py | grep -E "^검사|✗" && git commit` 꼴에서 grep 이 오류 줄을 찾아도 종료코드 0이라 커밋이 이어짐. 규칙(오류 0 확인 뒤 별도 명령으로 커밋)을 한 줄로 합치다 생긴 것.
- 고침: 다음 커밋에서 문장 수정. 이후로는 check 를 따로 돌려 「오류 0」을 본 뒤에만 커밋 명령을 낸다(배포.md 규칙 그대로).
