# 경로대장 — 광고관리자(사장님 마케팅 교실)

> 여기 있는 값은 다시 묻지 않는다. 안 맞아 보이면 그때 묻는다. (전역 규칙 1-1)

## 레포·브랜치

| 항목 | 값 |
|---|---|
| 레포 | `scg5376-beep/github` (public, 설명 "사용법") |
| 로컬 | `C:\Users\USER\Desktop\광고관리자` |
| 작업 브랜치 | `work` (원격 `origin/work`) — 여기서 작업 |
| 배포 브랜치 | `main` — `work` 를 ff-merge 해서 올리면 `.github/workflows/pages.yml` 이 `site/` 를 배포 |
| 옛 브랜치 | `claude/video-ai-masterpiece-workflow-jfds1o` — `work` 의 출발점. 더 안 쓴다 |
| 레포 안 프로젝트 셋 | 루트 = 마스터피스 스튜디오(영상 AI 자산) · `marketing-doctor/` = 마케팅 지식창고(컨트롤 타워) · `site/` = 교육 사이트 |

## 도메인·DNS·호스팅 (전부 무료 요금제)

| 항목 | 값 |
|---|---|
| 도메인 | `sajangmarketing.com` — Cloudflare Registrar, 2026-09-11 등록, 1년, 자동 갱신 ON (US$10.46/년) |
| WHOIS | Cloudflare 가 자동 가림(redaction). 등록자 정보는 운영자 개인 정보 — **외부에 캡처 공유 금지** |
| DNS | Cloudflare (계정 `scg5376@gmail.com`, account id `3905f7ab74f2e6116a78361c619bfb7d`) |
| DNS 레코드 | A `@` → 185.199.108.153 / .109 / .110 / .111 (GitHub Pages) · CNAME `www` → `scg5376-beep.github.io` · TXT `_github-pages-challenge-scg5376-beep` → GitHub 도메인 확인용. **전부 DNS only(회색 구름)** — 프록시 켜면 GitHub 인증서 발급이 막힐 수 있다 |
| DNSSEC | 2026-09-11 켬 (Registrar 가 DS 자동 등록, 반영 1~2일) |
| 호스팅 | GitHub Pages, build_type=workflow, 커스텀 도메인 `sajangmarketing.com` (`site/CNAME`) |
| 임시 주소 | `https://scg5376-beep.github.io/github/` — 이제 `https://sajangmarketing.com` 으로 301 |
| GitHub 도메인 확인 | github.com/settings/pages → `sajangmarketing.com` **Verified** (2026-09-11 03:30). TXT 레코드는 지우지 않는다 |
| HTTPS 강제 | **켬** (2026-09-11). 인증서 발급 확인, `https_enforced: true` |

## 아직 운영자가 직접 해야 하는 것 (계정·본인 인증)

- Cloudflare 계정 2단계 인증 (My Profile → Authentication)
- GitHub 계정 2단계 인증
- 네이버 서치어드바이저 · 구글 서치 콘솔 소유확인 — 코드는 `site/_build/verify.json` 에 `{"naver": "...", "google": "..."}` 로 넣으면 빌드가 메인 head 에 박는다

## 세션 도구 상태 (2026-09-11)

- 브라우저 확장(claude-in-chrome): 운영자가 켜 주면 붙는다. Cloudflare 대시보드는 CDP 스크린샷이 자주 멈춘다 → `find`/`javascript_tool` 로 상태 읽는 편이 안정적
- codex MCP: 레포 루트 `.mcp.json` 에 등록 (SECOND-HOME 과 같은 설정, `codex mcp-server`). **세션을 새로 열어야 붙고, 처음 한 번 프로젝트 MCP 승인 프롬프트가 뜬다.** 2026-09-11 세션은 이 파일이 없어서 이미지-코덱스가 못 돌았다

## 사이트 작업 규칙 (요약 — 어떤 일에 어떤 규칙은 `docs/길라잡이.md`, 규칙 본문은 `docs/규칙/`)

| 무엇 | 어디 |
|---|---|
| 발전 루프 | `docs/규칙/발전.md` · 큐/일지/예산 `docs/발전/` · 예산 `python site/_build/budget.py --start/--check/--calibrate/--cap N`(이 프로젝트 토큰 수, 5시간 25% 상당; 상태줄 %는 `~/.claude/statusline-rate.json`) · 토큰 참고 `usage.py` (세션 폴더 `~/.claude/projects/C--Users-USER-Desktop------/`) |
| 문체 참고 저장소 | `참고/im-not-ai` ← `https://github.com/epoko77-ai/im-not-ai.git` · `참고/fluent-korean` ← `https://github.com/snflkd/fluent-korean` (운영자 추천 2026-09-19). `참고/` 는 `.gitignore`. 출력 스타일 사본 `.claude/output-styles/fluent-korean.md`(`/config` 에서 고름) |
| 네이버 도움말 번호 목록 | `marketing-doctor/지식/원전/원문/절차/네이버플레이스/_도움말-번호-목록.md` (109건, 창고 보유 표시). 새 도움말은 `contents/<번호>?osType=COMMONOS` + 15초 대기로 받는다 |
| 규칙(현재) | `docs/길라잡이.md` → `docs/규칙/*.md` (공통_시작·공통_마무리·보안·글쓰기·방법글·설명글·근거·레이아웃·캡처·배포) |
| 설계기준(결정 역사) | `site/docs/설계기준.md` |
| 방법 글 생성기·캡처·검사 도구 | `site/_build/howto/`(regen.py) · `site/_build/shots.py`·`shot.mjs`·`mark.py` · `audit.mjs`·`scroll.mjs`·`mkurls.py` · `cleanup.py`(좀비 정리) |
| 자동 검사(2겹) | `python site/_build/build.py && python site/_build/check.py` — 오류 0 이어야 커밋 |
| CI·사람 검수(3겹) | `.github/workflows/pages.yml` · `site/docs/검수기록.md` |
| 글 원본 | `site/_src/pages/**` (빌드 결과도 같이 커밋) |
| 소유확인 코드 | `site/_build/verify.json` 에 `{"naver": "...", "google": "..."}` |
| 코덱스 (MCP 없이) | `marketing-doctor/엔진/codex_exec.ps1` — `Start-Process` 로 분리 실행. 셸 명령은 못 쓰니 「이미지 생성 도구만 쓰고 결과는 최종 답변에」로 브리프. 이미지는 `~/.codex/generated_images/<실행ID>/` — 실행마다 ID 를 로그로 확인(다른 세션 이미지와 섞임 주의) |
| 공식 문서 원문 받기 | `marketing-doctor/엔진/원문받기/` — WebFetch 가 막힌 도메인도 헤들리스 Edge 로 받는다 (README) |
| 광고 켜기/끄기 | `site/_build/site.json` — `ads.enabled` true + `adsense_client`(ca-pub-…) + `slots` 4개. 기본 false. 켜면 CSP 빠지고 `/privacy.html` 이 전제 |

- 코덱스 MCP: 2026-09-12 세션에서 `mcp__codex__codex` 정상(이미지·CSS). 파일 읽기 도구는 없음 → 자료는 프롬프트에 직접. 산출 이미지 `~/.codex/generated_images/<threadId>/`.
- 헤드리스 캡처: `site/_build/scroll.mjs`(뷰포트+스크롤), `audit.mjs`(전수). 프로필 재사용 시 이전 탭이 찍히므로 실행 전 `taskkill //F //IM msedge.exe`.
- 구글 서치콘솔: 속성 `https://sajangmarketing.com/` (URL 접두어), HTML 태그로 소유확인 2026-09-14, 사이트맵 /sitemap.xml 제출(83페이지 발견). 코드는 `site/_build/verify.json`.
- 네이버 서치어드바이저: 확장 프로그램이 naver.com을 막음 → 운영자가 직접 소유확인·사이트맵(sitemap.xml, feed.xml) 제출 완료(2026-09-14). 이후 수집 현황도 운영자 화면에서 확인.
- VS Code 자동 세션: `.vscode/tasks.json` — 폴더를 열면 터미널에서 `claude --continue`(가장 최근 세션 이어서). 처음 한 번 「Allow Automatic Tasks」 허용 필요. 특정 세션을 고정하려면 `claude --resume <세션ID>`로 바꾼다.

| 구글 애널리틱스 4 | 측정 ID 는 `site/_build/site.json` → `analytics.ga4_id` (G-…). 비어 있으면 꺼짐. 발급: analytics.google.com > 관리 > 속성 만들기 > 데이터 스트림(웹, sajangmarketing.com) > 측정 ID. 운영자 로그인 필요 (2026-09-18) |
| GA4 동작 확인 | `node site/_build/ga_probe.mjs https://sajangmarketing.com/` → GA requests 2·errors 0 이면 정상 |
- 스마트스토어 안전거래 가이드(이용규칙·판매관리 페널티·상품등록 정책): https://safety.smartstore.naver.com/main/rules/safety/credit — curl 로 받아짐(2026-09-20). 원문 `절차/네이버기타/스마트스토어-안전거래가이드-*.md`
- 구글 비즈니스 프로필 고객센터: https://support.google.com/business/answer/<번호>?hl=ko — curl 로 받아지고 `<article>` 안이 본문(2026-09-20). 원문 `원문/구글/GB-*.md`
- 예약주문 사업자 고객센터(service/11712) 도움말 번호 목록: `marketing-doctor/지식/원전/원문/절차/네이버예약/_예약주문-도움말-번호-목록.md` (146건, 2026-09-20). 번호는 네이버 웹문서 검색(PlayMCP)으로 모았고 category 페이지는 JS 라 fetch_links 불가.
- 네이버 톡톡파트너센터 고객센터: help.naver.com/service/11830/contents/<번호>. 원문 `절차/네이버톡톡/` (2026-09-20, 11건 + 번호 목록 16)
- 네이버 클립 고객센터: help.naver.com/service/30048/contents/<번호>. 원문 `절차/네이버클립/` (2026-09-20, 5건)
- MY플레이스 고객센터(손님 쪽, 사업자 항목 포함): help.naver.com/service/19485/contents/<번호>. 원문 `절차/네이버플레이스/MY플레이스-*.md` (2026-09-20: 별점 25093·업체 페널티 25056·어뷰징 24961·보호정책 12377·타업체 리뷰 11549·영수증 기준 9766)

- 구글 비즈니스 프로필 고객센터: https://support.google.com/business/?hl=ko (curl 됨, User-Agent 필요). 번호 목록 `marketing-doctor/지식/원전/원문/구글/GB-00-도움말-번호-목록.md` (100건, 2026-09-20). 색인 글 `/guide/google-help-index.html`.
- 당근비즈니스 가이드 전체 목록: sitemap-pages.xml 로 118건 (curl 됨). 목록 `marketing-doctor/지식/원전/원문/절차/당근/_가이드-문서-목록.md`. 색인 글 `/guide/daangn-help-index.html` (2026-09-20).
- 헤들리스 원문 받기 도구: `marketing-doctor/엔진/원문받기/fetch_text.mjs URL out.txt 15000` (site/_build 에는 없음. help.naver.com 은 ?osType=COMMONOS 붙임)
- 유튜브 고객센터(크리에이터): https://support.google.com/youtube/topic/<번호>?hl=ko (curl 됨). 번호 목록 `marketing-doctor/지식/원전/원문/유튜브/YT-00-도움말-번호-목록.md` (404건, 2026-09-20). 색인 글 `/guide/youtube-help-index.html`.
- 네이버 검색 고객센터: https://help.naver.com/service/5626/contents/<번호> (헤들리스 필요, ?osType=COMMONOS). 가게 관련 83건 목록 `marketing-doctor/지식/원전/원문/네이버/NV-09-검색-고객센터-도움말-번호-목록.md`. 색인 글 `/guide/search-help-index.html` (2026-09-20). 핵심: 19891 웹 검색 노출 · 8026 웹문서 수집·삭제 정책 · 8022 노출 순위 기준 · 15214 검색로봇 · 24119 AI 브리핑.
- MY플레이스 고객센터(손님 쪽 리뷰): https://help.naver.com/service/19485/contents/<번호> (헤들리스 필요). 사장님 관련 51건 목록 `marketing-doctor/지식/원전/원문/절차/네이버플레이스/_MY플레이스-도움말-번호-목록.md`. 색인 글 `/guide/myplace-help-index.html`. 미션은 2026-03-01 종료(20201 안내문).
- 서치어드바이저 수집요청 자동 제출: `site/_build/sa.mjs` (`open` → 운영자가 그 Edge 창에서 로그인 → `submit 목록.txt [n]`). 프로필 `%TEMP%/edge-sa`, 포트 9444. 입력칸에는 전체 주소(https://…)를 넣어야 하고 결과는 「수집 요청 내역」 1번 줄로 확인. 크롬 확장은 naver 도메인을 자체 차단해 못 씀(2026-09-20).
