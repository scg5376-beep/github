# 길라잡이 — 어떤 일을 할 때 어떤 규칙을 읽나 (최상위, 2026-09-16)

> 운영자 지시(2026-09-16): "작업들을 세분화시켜서 봐야할 규칙을 가진 경로를 독립적으로 지정하고 최상위규칙 길라잡이를 만들어서 길라잡이가 어떤 작업을 할때는 어떤 규칙을 보고 하게끔 경로를 지정 … 그 길라잡이에 마지막 검수 및 이렇게 좀비프로세스 관리 규칙도 넣어놓고"
>
> **이 파일에는 표만 있다.** 규칙 본문은 `docs/규칙/` 아래 파일 하나에 일 하나. 규칙을 고칠 땐 그 파일만 고친다. 여기엔 경로만 더한다.
> 전역 규칙(`~/.claude/CLAUDE.md`)은 그대로 위에 있다. 레포 루트 `CLAUDE.md` 는 이 파일을 가리키기만 한다.

## 0. 예외 없이 — 모든 일

| 언제 | 읽는 파일 |
|---|---|
| **시작 전** | `docs/규칙/공통_시작.md` (경로대장 → 작업일지 3일 → 아래 표에서 일 찾기 → 도구 상태) |
| **모든 일에 겹쳐서** | `docs/규칙/보안.md` (돈·개인정보·브랜드 중립·JS 없음·로그인 금지 — 짧다) |
| **끝낼 때** | `docs/규칙/공통_마무리.md` (최종 검수 → 기록 → **좀비 프로세스 정리** → 보고) |

## 1. 일 → 규칙 파일

| 일 | 이런 말이 나오면 | 읽는 파일 (순서대로) |
|---|---|---|
| 글의 말투·문장 | 문장이 이상하다·AI 티·자연스럽게·다시 써 | `.claude/skills/ko-tone/SKILL.md` → `docs/규칙/글쓰기.md` |
| 방법 글(코스 33편) | 따라 하기·단계·순서·플레이스 등록 방법·이유 상자 | `docs/규칙/방법글.md` → 글쓰기 |
| 설명 글(개념·효과) | 왜·원리·근거·연구·사례·개념 글 | `docs/규칙/설명글.md` → 글쓰기 → 근거 |
| 용어·업데이트 페이지 | 용어 추가·규칙 바뀐 것·업데이트 | `docs/규칙/설명글.md` §용어·업데이트 → 근거 |
| 사실·숫자·인용·원문 | 출처·인용·원문 받아·창고·대조 | `docs/규칙/근거.md` → `marketing-doctor/CLAUDE.md` |
| 레이아웃 | 첫 화면·메뉴·목록·상자·표·줄바꿈·CSS·모바일·자가진단 | `docs/규칙/레이아웃.md` |
| 화면 캡처 | 캡처·스크린샷·빨간 표시·화살표·길라잡이 그림 | `docs/규칙/캡처.md` → 방법글 |
| 빌드·검사·커밋·배포 | 올려·배포·커밋·검사 오류 | `docs/규칙/배포.md` |
| 이미지·영상 생성 | 마스터피스·코덱스·힉스필드·릴스·클링 | `AGENTS.md`(레포 루트) → 전역 규칙 4 |
| 지식창고 손보기 | marketing-doctor·경로표·문체 말뭉치 | `marketing-doctor/CLAUDE.md` |
| 다른 저장소 학습 | 클론해서 학습·비교해 봐 | 스크래치패드에 클론 → 노트는 `marketing-doctor/지식/문체/` 또는 `조사/` → 적용은 운영자 확인 뒤 |

## 2. 규칙 파일이 지켜야 할 모양

- 파일 하나 = 일 하나. **한 화면(120줄 안)** — 넘치면 쪼개고 이 표에 줄을 더한다
- 맨 위에 「언제 읽나」, 안에 「어디에 무엇(경로)」·「지켜야 할 것」·「도구」·「고칠 때」
- 다른 파일 내용을 베끼지 않고 **경로로 가리킨다**(같은 규칙이 두 곳이면 한쪽이 낡는다). 말투 규칙은 ko-tone 한 곳, 수치는 `spec.json`·`tone.json` 한 곳
- 새 규칙: 해당 파일에 넣고 → 이 표에 경로가 없으면 한 줄 추가 → `site/docs/설계기준.md` 에 D 번호로 결정 기록 → 작업일지에 「규칙 추가」 한 줄
- 규칙끼리 부딪히면: 보안 > 근거 > 글쓰기 > 레이아웃 순. 운영자 지시가 규칙과 다르면 한 문장으로 걱정을 말하고, 다시 말하면 지시대로 하고 D 번호로 남긴다
- 도구(스크립트)는 **레포 안** `site/_build/` 에. 스크래치패드는 세션이 끝나면 사라진다

## 3. 역사와 기록은 어디에

| 무엇 | 경로 |
|---|---|
| 설계 결정과 이유(D1~D41…) — 역사 장부. 현재 규칙과 다르면 규칙 파일이 맞다 | `site/docs/설계기준.md` |
| 사람 검수 기록 | `site/docs/검수기록.md` |
| 하루 작업(운영자 말 그대로) | `docs/작업일지/YYYY-MM-DD.md` |
| 경로·계정·값 | `docs/경로대장.md` |
